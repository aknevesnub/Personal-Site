# andreknopp.com.br

Site pessoal de André Knopp — React + Vite + Tailwind CSS.

## Rodar localmente

```bash
npm install
npm run dev
```

Abre em http://localhost:3000.

## Deploy

Push na branch `main` publica automaticamente via Vercel em https://andreknopp.com.br.
